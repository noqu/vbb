Dieses Verzeichnis enthaelt alle Dateien zum sequentiellen Kern,
wie er auf der Sun lauffaehig ist.  'make' erzeugt hier die Datei 
'sun_kernel.a', die den sequentiellen Kern als Library zum Binden 
mit Instanzfunktionen enthaelt.
(Es wurde ein eigenes Verzeichnis verwendet, weil sich die
Compiler-Aufrufe und damit das Makefile grundlegend von der
Transputer-Version unterscheidet. Der Quellcode ist identisch 
mit der Transputer-Version und wird ueber einen symbolischen 
Verweis von dort mitbenutzt.)

makefile	Lokales Makefile, wird von 'make' aufgerufen.

obj/		Hier legt 'make' Objektdateien an.

sun_kernel.a	Wird von 'make angelegt; enthaelt den fertigen
		parallelen Kern.

src@		Symbolischer Verweis auf den Quellcode des
		sequentiellen Kerns in ../seq/src
